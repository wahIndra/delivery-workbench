/**
 * Business logic services.
 * Services enforce all business rules (BR-01 to BR-10) and security governance rules.
 * AI is only invoked through AIService interface — never directly from controllers.
 */
@NonNullApi
package com.deliveryworkbench.service;

import org.springframework.lang.NonNullApi;
