package com.deliveryworkbench;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * IT Delivery Workbench — AI-assisted delivery governance platform.
 *
 * <p>AI is an assistant only. Humans retain full accountability for all approvals,
 * design decisions, test sign-offs, and release decisions.
 */
@SpringBootApplication
public class DeliveryWorkbenchApplication {

    public static void main(String[] args) {
        SpringApplication.run(DeliveryWorkbenchApplication.class, args);
    }
}
